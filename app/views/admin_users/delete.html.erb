<% @page_title = 'Delete a new AdminUser' %> 
<%= link_to "Admin Index", action:'index' %> 

<%= form_for @admin_user, url: {action: 'destroy', id: @admin_user.id} do |f| %>
<%= render partial: 'form', locals: {:f => f} %> 
<%= f.submit "Delete", class: 'btn btn-danger' %>

<% end %> 